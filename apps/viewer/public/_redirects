# Rewrite /s/assets/* to /assets/* (must be before SPA fallback)
/s/assets/*  /assets/:splat  200

# ZoneWise Chat UI - standalone page
/chat  /chat.html  200

# SPA routing - serve index.html for /s/* routes
/s/*  /index.html  200

# Marketing pages
/login  /index.html  200
/signup  /index.html  200
/dashboard  /index.html  200

# API routes are handled by functions (automatic in Cloudflare Pages)
