{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo PWD=$PWD && ls -la && echo --- && ls apps/ 2>/dev/null || echo NO_APPS_DIR && echo --- && ls -la /vercel/path0/ 2>/dev/null | head -20",
  "outputDirectory": "apps/viewer/dist",
  "installCommand": "bun install",
  "framework": null
}
